"""Experimental / not-validated scaffolding — never mistake this for a domain.

Modules here are **not** rigorous Morphogen domains. They are API sketches whose
numbers are fabricated placeholders, kept out of ``morphogen.stdlib``'s domain
namespace and its domain count so they cannot be confused with the analytically
validated domains. Import a module explicitly if you specifically want the stub:

    from morphogen.stdlib.experimental import qchem   # returns fabricated numbers

Contents:
- ``qchem`` — quantum-chemistry interface sketch; every energy/force/frequency
  function returns a placeholder and warns (BACKLOG P0-3).
"""
